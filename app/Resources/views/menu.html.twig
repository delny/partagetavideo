<nav class="navbar navbar-default navbar-fixed-top" role="navigateur">
    <div class="container">
        <div class="navbar-header logo_menu">
            <a href="{{ path('homepage') }}" class="navbar-brand"><span class="glyphicon glyphicon-film"></span><span>Partage ta vid&eacute;o</span></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            {% if app.user %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('app_new_video') }}">Ajouter une vid&eacute;o</a></li>
                    <li><a href="{{ path('app_profil_view', {'username':app.user.username } ) }}">Page profil</a></li>
                    <form method="post" action="{{ path('app_search') }}" class="navbar-form navbar-left " role="search">
                        <div class="form-group navbar-form-search">
                            <input type="text" name="nom" class="form-control input-search-navbar" placeholder="Rechercher un utilisateur, une vidéo">
                            <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
                        </div>
                    </form>
                    <li class="dropdown">
                        <a href="" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-cog"></span> Réglages<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('fos_user_profile_show') }}">Paramètres</a></li>
                            <li><a href="{{ path('app_list_fav') }}">Mes favoris</a></li>
                            <li class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                        </ul>
                    </li>
                </ul>
            {% else %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('homepage') }}">Accueil</a></li>
                    <form method="post" action="{{ path('app_search') }}" class="navbar-form navbar-left " role="search">
                        <div class="form-group navbar-form-search">
                            <input type="text" name="nom" class="form-control input-search-navbar" placeholder="Rechercher un utilisateur, une vidéo">
                            <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
                        </div>
                    </form>
                    <li><a href="{{ path('fos_user_registration_register') }}">S'inscrire</a></li>
                    <li><a href="{{ path('fos_user_security_login') }}">Se connecter</a></li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>

